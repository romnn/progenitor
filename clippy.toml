# Runtime and tool crates deny unwrap/expect/panic through the workspace lint
# policy. progenitor-impl keeps an explicit, documented exception list for
# validated generation invariants. Tests may use these failure modes directly.
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-panic-in-tests = true
allow-indexing-slicing-in-tests = true
