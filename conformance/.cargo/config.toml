# Cap parallel jobs to avoid OOM on monster specs.
[build]
jobs = 8

# Faster linker for the dev profile. Disable or change to "lld" if mold is
# not installed.
# [target.x86_64-unknown-linux-gnu]
# linker = "clang"
# rustflags = ["-C", "link-arg=-fuse-ld=mold"]
